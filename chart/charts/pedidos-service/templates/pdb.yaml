{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "pedidos-service.fullname" . }}
  labels:
    {{- include "pedidos-service.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "pedidos-service.selectorLabels" . | nindent 6 }}
{{- end }}
