# Valores por defecto de reportes-service.
# Los valores especificos de ambiente van en values-dev.yaml / values-prod.yaml
# del chart PADRE, bajo la clave "reportes-service:".

# Permite instalar el chart padre sin este subchart todavia
# (--set reportes-service.enabled=false) mientras se termina el resto.
enabled: true

replicaCount: 1

# Rolling update sin caida de servicio: maxUnavailable=0 obliga a que el
# pod nuevo pase su readinessProbe ANTES de tumbar el viejo (nunca hay
# menos replicas listas que las que habia). maxSurge=1 es lo que permite
# esto incluso con replicaCount=1 (dev): durante el rollout existen
# temporalmente 2 pods (el viejo + el nuevo) en vez de escalar a 0 primero.
strategy:
  type: RollingUpdate
  maxUnavailable: 0
  maxSurge: 1

image:
  repository: p5-reportes-service
  pullPolicy: IfNotPresent
  tag: "runtime"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Ahora la crea Terraform (P8/terraform/rbac.tf), no Helm — evita que dos
  # dueños (Terraform y Helm) compitan por el mismo objeto.
  create: false
  # Ninguno de los 6 microservicios llama a la API de Kubernetes desde su
  # codigo (no usan ningun cliente de Kubernetes) — no hay razon para que
  # el pod cargue un token que nunca va a usar. El Role/RoleBinding de
  # este subchart (ver templates/role.yaml) siguen aplicando a la
  # identidad de la ServiceAccount independientemente de esto.
  automount: false
  annotations: {}
  name: "reportes-service"

podAnnotations: {}
podLabels: {}

# Config no sensible inyectada via ConfigMap (ver templates/configmap.yaml).
# DB_PASSWORD y JWT_SECRET NO van aqui — vienen de Secrets externos
# (el "postgres" de Bitnami y el "sa-p5-shared-secrets" del chart padre).
config:
  logLevel: "info"
  dbHost: "postgres"
  dbPort: 5432
  dbUser: "postgres"
  dbName: "reportes_db"
  authzServiceUrl: "http://authz-service:4000"
  authzMaxRetries: 3
  authzRetryBackoffMs: 300
  productosServiceUrl: "http://productos-service:8000"
  # Comunicacion asincrona: Cronjob 2 publica, el consumidor de este mismo
  # subchart consume. rabbitmqUser debe coincidir con "rabbitmq.auth.username"
  # del values.yaml del chart PADRE (no hay forma de referenciarlo cruzado,
  # hay que mantenerlo sincronizado a mano).
  rabbitmqHost: "rabbitmq"
  rabbitmqPort: 5672
  rabbitmqUser: "sa_p5"
  rabbitmqQueue: "resumenes-cronjob"
  # Cronjob 1: numero de carne insertado en cada heartbeat.
  studentCarne: "202300547"

podSecurityContext: {}

securityContext:
  runAsNonRoot: true
  # El Dockerfile hace "USER app" (nombre, no UID numerico). Kubernetes no
  # puede resolver ese nombre sin ejecutar el contenedor, asi que
  # runAsNonRoot por si solo no basta — hay que fijar el UID numerico que
  # ese usuario tiene DENTRO de la imagen (confirmado con `docker run ... id`).
  runAsUser: 999
  runAsGroup: 999
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false
  capabilities:
    drop: ["ALL"]

service:
  type: ClusterIP
  port: 8000

resources:
  requests:
    cpu: 75m
    memory: 128Mi
  limits:
    cpu: 250m
    memory: 256Mi

livenessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 5
  periodSeconds: 10
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 3
  periodSeconds: 5
  failureThreshold: 3

# El pool de psycopg2 se crea de forma sincrona al importar
# src/database/connection.py (hasta 10 intentos x 2s antes de que Uvicorn
# arranque). En un cold-start completo del stack (los 9 pods compitiendo
# por CPU a la vez), eso puede superar un presupuesto de 30s.
startupProbe:
  httpGet:
    path: /health
    port: http
  periodSeconds: 5
  failureThreshold: 20

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70

# Minimo de pods vivos garantizado durante una disrupcion VOLUNTARIA
# (kubectl drain, upgrade de nodo, cluster-autoscaler) — no aplica a
# rollouts normales de Deployment, que no pasan por la Eviction API.
# minAvailable: 1 con replicaCount: 1 (ambiente dev) bloquea CUALQUIER
# eviccion mientras solo haya 1 replica viva; con 2+ (prod, o cuando el
# HPA escala) ya permite drenar nodos uno a la vez sin dejar el servicio
# en cero.
podDisruptionBudget:
  enabled: true
  minAvailable: 1

volumes: []
volumeMounts: []

nodeSelector: {}
tolerations: []
affinity: {}

# --- Cronjobs encadenados (punto H del enunciado) ---
cronjobs:
  heartbeat:
    # Cada 2 minutos.
    schedule: "*/2 * * * *"
    timeZone: "America/Guatemala"
    concurrencyPolicy: Forbid
    backoffLimit: 2
    successfulJobsHistoryLimit: 3
    failedJobsHistoryLimit: 3
    resources:
      requests:
        cpu: 25m
        memory: 32Mi
      limits:
        cpu: 100m
        memory: 64Mi
  resumen:
    # Cada 10 minutos.
    schedule: "*/10 * * * *"
    timeZone: "America/Guatemala"
    concurrencyPolicy: Forbid
    backoffLimit: 2
    successfulJobsHistoryLimit: 3
    failedJobsHistoryLimit: 3
    resources:
      requests:
        cpu: 25m
        memory: 32Mi
      limits:
        cpu: 100m
        memory: 64Mi

# --- Consumidor asincrono de la cola "resumenes-cronjob" (punto D) ---
# Deployment de larga duracion, separado del servidor HTTP, misma imagen.
consumer:
  replicaCount: 1
  # No expone HTTP, asi que sus probes son "exec": revisan que este
  # archivo de latido (que el propio proceso refresca cada 10s, ver
  # src/scripts/consumer_resumenes.py) no tenga una antiguedad mayor a
  # "freshnessSeconds". Vive en un emptyDir montado en /health porque el
  # resto del pod corre con readOnlyRootFilesystem: true.
  heartbeatPath: /health/consumer-healthy
  startupProbe:
    periodSeconds: 5
    failureThreshold: 20
    freshnessSeconds: 60
  livenessProbe:
    periodSeconds: 10
    failureThreshold: 3
    freshnessSeconds: 30
  readinessProbe:
    periodSeconds: 10
    failureThreshold: 3
    freshnessSeconds: 30
  resources:
    requests:
      cpu: 50m
      memory: 96Mi
    limits:
      cpu: 200m
      memory: 192Mi
